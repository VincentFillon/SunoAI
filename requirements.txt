# Suno AI Prompt Generator — dependencies
PySide6>=6.6.0
openai>=1.0.0
anthropic>=0.40.0
google-genai>=1.0.0
keyring>=24.0.0

# Packaging (optional, only needed to build the standalone executable)
pyinstaller>=6.0.0
